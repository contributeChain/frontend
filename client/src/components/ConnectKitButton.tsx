import { RainbowKitButton, CustomRainbowKitButton } from './RainbowKitButton';

// Renamed from ConnectKitButton to avoid confusion with the original component
export function ConnectKitButton() {
  // Now using RainbowKit instead of the placeholder
  return <CustomRainbowKitButton />;
} 